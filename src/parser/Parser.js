const { Tokeniser } = require('../lexer/Tokeniser');
const { Grammar } = require('../grammar/Grammar');

class Parser {
    
}

const stringLiteral = s => target => {
    
}

module.exports = { Parser };